import { createMuiTheme } from "@material-ui/core/styles";

const palette = createMuiTheme({
  palette: {
    primary: { main: "#142850" },
    secondary: { main: "#00909e" },
    background: { default: "#dae1e7" },
  },
});

export default palette;
